import type { Asset } from "@/types";

// This references the attached PDF via the Vite alias setup in vite.config.ts
const cvPdf: Asset = {
  path: "@assets/SoufianeElQasemy CV.pdf",
  filename: "SoufianeElQasemy-CV.pdf"
};

export default cvPdf;
